import SectionLabel from '@/components/ui/SectionLabel'
import SafeImage from '@/components/ui/SafeImage'
import type { Capability } from '@/data/services'
import { Check } from 'lucide-react'
export default function ServiceCapabilities({capabilities,image,name}:{capabilities:Capability[];image:string;name:string}){return <section id="scope" className="bg-paper py-20 lg:py-28"><div className="container-a"><SectionLabel index="01">WHAT WE COVER</SectionLabel><div className="grid lg:grid-cols-[.72fr_1.28fr] gap-10 lg:gap-16 items-start"><div className="lg:sticky lg:top-28"><h2 className="h-display text-4xl sm:text-5xl max-w-xl">A clear scope, without the jargon.</h2><p className="text-mutedDark leading-7 mt-6 max-w-md">{name} is shaped around the environment you already have. The list below shows the typical areas we cover; the exact scope is agreed before work starts.</p><div className="relative aspect-[4/3] rounded-2xl overflow-hidden mt-9 border border-lineDark"><SafeImage src={image} alt={name} className="w-full h-full object-cover"/><div className="absolute inset-0 bg-gradient-to-t from-ink/60 to-transparent"/></div></div><div className="grid sm:grid-cols-2 gap-3">{capabilities.map(c=><div key={c.title} className="rounded-2xl border border-lineDark bg-white p-6 sm:p-7"><p className="font-display text-xl">{c.title}</p><ul className="mt-5 space-y-3">{c.items.map(item=><li key={item} className="flex items-start gap-2.5 text-sm text-mutedDark leading-6"><Check size={15} className="text-signal shrink-0 mt-1"/>{item}</li>)}</ul></div>)}</div></div></div></section>}
