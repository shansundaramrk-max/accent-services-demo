import { motion } from 'framer-motion'
import { Link } from 'react-router-dom'
import { ChevronRight, ArrowDownRight } from 'lucide-react'
import Button from '@/components/ui/Button'
import SafeImage from '@/components/ui/SafeImage'
import { categoryMeta, getServiceImage, type Service } from '@/data/services'
export default function ServiceHero({service}:{service:Service}){const meta=categoryMeta[service.category];return <section className="bg-ink text-white relative overflow-hidden"><div className="absolute inset-0 dark-grid opacity-50"/><div className="container-a relative py-14 sm:py-20 lg:py-24"><div className="flex items-center gap-2 text-white/35 text-xs mb-10"><Link to="/solutions" className="hover:text-signal">Solutions</Link><ChevronRight size={12}/><span>{meta.label}</span></div><div className="grid lg:grid-cols-[1.05fr_.95fr] gap-12 lg:gap-20 items-end"><div><p className="eyebrow mb-5">{meta.label}</p><h1 className="h-display text-4xl sm:text-5xl lg:text-6xl text-balance max-w-3xl">{service.tagline}</h1><p className="text-white/55 text-base sm:text-lg leading-8 max-w-2xl mt-7">{service.summary}</p><div className="mt-9 flex flex-wrap gap-3"><Button to="/contact">Discuss this service</Button><a href="#scope" className="inline-flex items-center gap-2 rounded-full border border-white/10 px-5 py-3 text-sm text-white/70 hover:text-white hover:border-white/25">See what's covered <ArrowDownRight size={16}/></a></div></div><motion.div initial={{opacity:0,scale:.98}} animate={{opacity:1,scale:1}} transition={{duration:.7}} className="relative aspect-[4/3] lg:aspect-[5/4] rounded-[24px] overflow-hidden border border-white/10"><SafeImage src={getServiceImage(service)} alt={service.name} className="w-full h-full object-cover"/><div className="absolute inset-0 bg-gradient-to-t from-ink/75 via-ink/10 to-transparent"/><div className="absolute bottom-5 left-5 right-5 flex items-end justify-between"><div><p className="font-mono text-[9px] uppercase tracking-[.18em] text-white/45">Service</p><p className="font-display text-xl mt-1">{service.name}</p></div><span className="rounded-full border border-white/15 bg-ink/50 px-3 py-1.5 font-mono text-[9px] text-white/60">{service.capabilities.length} capability areas</span></div></motion.div></div></div></section>}
